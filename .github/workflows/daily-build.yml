name: 日常构建

on:
  push:
    branches:
      - '**'
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # ------------ 提前准备（读取包名 & 生成 tag & 读取版本号） ------------
  prepare:
    name: 准备构建信息
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
      datetime: ${{ steps.tag.outputs.datetime }}
      pkg: ${{ steps.pkg.outputs.pkg }}
      version: ${{ steps.ver.outputs.version }}

    steps:
      - name: 检出代码
        uses: actions/checkout@v5

      - name: 读取 pubspec.yaml 包名
        id: pkg
        run: |
          pkg=$(grep '^name:' pubspec.yaml | awk '{print $2}')
          echo "🔍 检测到包名: $pkg"
          if [ -z "$pkg" ]; then
            echo "::error::无法从 pubspec.yaml 找到包名"
            exit 1
          fi
          echo "pkg=$pkg" >> $GITHUB_OUTPUT

      - name: 读取 pubspec.yaml 主版本号
        id: ver
        run: |
          raw=$(grep '^version:' pubspec.yaml | awk '{print $2}')
          echo "🔍 原始版本号: $raw"

          if [ -z "$raw" ]; then
            echo "::warning::无法从 pubspec.yaml 找到 version 字段"
            echo "version=" >> $GITHUB_OUTPUT
            exit 0
          fi

          version="${raw%%+*}"

          echo "📦 主版本号: $version"
          echo "version=$version" >> $GITHUB_OUTPUT

      - name: 生成版本标签
        id: tag
        run: |
          datetime=$(date -u +"%Y%m%d%H%M")
          tag="v${datetime}"
          echo "⏱️ 构建时间: $datetime"
          echo "🏷️ 版本标签: $tag"
          echo "tag=$tag" >> $GITHUB_OUTPUT
          echo "datetime=$datetime" >> $GITHUB_OUTPUT

  # ------------ 构建 job（依赖 prepare） ------------
  build:
    name: 构建 Windows-${{ matrix.arch }}
    needs: prepare
    strategy:
      matrix:
        include:
          - arch: x64
            runs-on: windows-latest
          - arch: arm64
            runs-on: windows-11-arm
    runs-on: ${{ matrix.runs-on }}

    steps:
      - name: 检出代码
        uses: actions/checkout@v5

      - name: 输出构建信息（Debug）
        shell: pwsh
        run: |
          Write-Host "============================================"
          Write-Host "📦 包名:        ${{ needs.prepare.outputs.pkg }}"
          Write-Host "⏫ 版本号:      ${{ needs.prepare.outputs.version }}"
          Write-Host "🏷️ Tag:         ${{ needs.prepare.outputs.tag }}"
          Write-Host "🕒 时间:        ${{ needs.prepare.outputs.datetime }}"
          Write-Host "🖥️ 架构:        ${{ matrix.arch }}"
          Write-Host "============================================"

      - name: 配置 Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: master

      - name: 配置 Rust
        uses: dtolnay/rust-toolchain@stable

      - name: 安装 Flutter 依赖
        run: flutter pub get

      - name: 生成 Rust 桥接代码
        run: |
          cargo install rinf_cli
          rinf gen

      - name: 生成多语言文件
        run: dart run slang

      - name: 构建应用程序
        run: |
          flutter build windows --release -v
          flutter build windows --debug -v

      - name: 打包构建产物
        shell: pwsh
        env:
          TAG: ${{ needs.prepare.outputs.tag }}
          PKG: ${{ needs.prepare.outputs.pkg }}
          ARCH: ${{ matrix.arch }}
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null

          $pkg = $env:PKG
          $tag = $env:TAG
          $arch = $env:ARCH

          Write-Host "🔧 打包：包名=$pkg  Tag=$tag 架构=$arch"

          $releaseZip = "${pkg}-windows-${tag}-${arch}.zip"
          $debugZip   = "${pkg}-windows-${tag}-${arch}-debug.zip"

          Compress-Archive -Path "build/windows/$arch/runner/Release/*" `
            -DestinationPath "dist/$releaseZip"

          Compress-Archive -Path "build/windows/$arch/runner/Debug/*" `
            -DestinationPath "dist/$debugZip"

          Write-Host "📦 产物文件:"
          Write-Host " - dist/$releaseZip"
          Write-Host " - dist/$debugZip"

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: ${{ needs.prepare.outputs.pkg }}-windows-${{ matrix.arch }}-${{ needs.prepare.outputs.tag }}
          path: dist/*.zip
          retention-days: 7

  # ------------ Release（上传到发布页） ------------
  release:
    name: 发布 Release（自动上传产物）
    needs: [prepare, build]
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v5

      - name: 下载构建产物
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: 预发布构建产物
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ needs.prepare.outputs.tag }}
          name: 日常构建 ${{ needs.prepare.outputs.datetime }}
          body: |
            ## 🚀 日常自动构建

            **包名**: `${{ needs.prepare.outputs.pkg }}`
            **应用版本**: `${{ needs.prepare.outputs.version }}`
            **构建时间**: ${{ needs.prepare.outputs.datetime }}
            **提交哈希**: ${{ github.sha }}

          files: artifacts/**/*.zip
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
