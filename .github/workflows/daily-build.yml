name: 日常构建

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"  # 每天凌晨 0 点（UTC）
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: 构建 ${{ matrix.arch }} (${{ matrix.mode }})
    strategy:
      matrix:
        include:
          - arch: x64
            runs-on: windows-latest
            rust-target: x86_64-pc-windows-msvc
            mode: release
          - arch: x64
            runs-on: windows-latest
            rust-target: x86_64-pc-windows-msvc
            mode: debug
          - arch: arm64
            runs-on: windows-11-arm
            rust-target: aarch64-pc-windows-msvc
            mode: release
          - arch: arm64
            runs-on: windows-11-arm
            rust-target: aarch64-pc-windows-msvc
            mode: debug
    runs-on: ${{ matrix.runs-on }}

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 配置 Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: master
          cache: false

      - name: 配置 Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.rust-target }}

      - name: 安装 Flutter 依赖
        run: flutter pub get

      - name: 生成 Rust 桥接代码
        run: |
          cargo install rinf_cli
          rinf gen

      - name: 生成多语言文件
        run: dart run slang

      - name: 构建 Windows 版本 (${{ matrix.mode }})
        run: flutter build windows --${{ matrix.mode }} -v

      - name: 打包构建产物
        run: |
          $suffix = if ("${{ matrix.mode }}" -eq "debug") { "-debug" } else { "" }
          $zipName = "loopback_manager-windows-${{ matrix.arch }}$suffix.zip"
          $buildPath = if ("${{ matrix.mode }}" -eq "debug") { "build/windows/${{ matrix.arch }}/runner/Debug" } else { "build/windows/${{ matrix.arch }}/runner/Release" }
          cd $buildPath
          Compress-Archive -Path * -DestinationPath "../../../../../$zipName"
        shell: pwsh

      - name: 上传构建产物到 Actions
        uses: actions/upload-artifact@v4
        with:
          name: loopback_manager-windows-${{ matrix.arch }}-${{ matrix.mode }}
          path: loopback_manager-windows-${{ matrix.arch }}${{ matrix.mode == 'debug' && '-debug' || '' }}.zip
          retention-days: 90

  release:
    name: 创建 GitHub Release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: 检出代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 下载所有构建产物
        uses: actions/download-artifact@v4
        with:
          path: ./dist

      - name: 生成版本标签
        id: tag
        run: |
          datetime=$(date -u +"%Y.%m.%d.%H.%M")
          echo "tag=v${datetime}" >> $GITHUB_OUTPUT
          echo "datetime=${datetime}" >> $GITHUB_OUTPUT

      - name: 创建 Git 标签
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ steps.tag.outputs.tag }}
          git push origin ${{ steps.tag.outputs.tag }}

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: 日常构建 ${{ steps.tag.outputs.datetime }}
          body: |
            ## 🚀 日常自动构建

            **构建时间**: ${{ steps.tag.outputs.datetime }}
            **提交哈希**: ${{ github.sha }}

            ### 📦 下载
            - Windows x64 Release
            - Windows x64 Debug
            - Windows ARM64 Release
            - Windows ARM64 Debug

            ⚠️ 这是自动构建版本，可能包含未充分测试的功能。

            ---
            自动生成于 GitHub Actions
          files: ./dist/**/*.zip
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
